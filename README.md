# Claims Dashboard

A browser-based dashboard for logging and tracking item claims (damaged or missing goods).

## Setup

1. Copy `config.example.js` → `config.js` and add your OpenAI API key (optional)
2. Open `index.html` in any modern browser

No install or build step required.
